version: '3.9'

services:
  nestjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mytonswap_docs
    ports:
      - ${PORT}:3000
    env_file:
      - .env
    networks:
      - mytonswap_network

networks:
  mytonswap_network:
    external: true